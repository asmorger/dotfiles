- name: Bootstrap development environment
  hosts: localhost
  roles:
    - role: doom-emacs
  tasks:
  - name: Install pacakges with apt
    become: yes
    ansible.builtin.apt:
      name:
        - git
        - fish
        - ripgrep
        - fd-find
        - emacs
        - emacs-common
        - eza
        - btop
      state: present
